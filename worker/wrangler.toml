name = "sage-worker"
main = "index.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
ALLOWED_ORIGINS = "https://sage-next-gen.pages.dev,http://localhost:5173,http://localhost:5174,http://localhost:4173"

# 生产环境 Secret（通过 wrangler secret put BAILIAN_API_KEY 设置）
# BAILIAN_API_KEY = 不在此处设置
